/*
   file name: 5.cm
   Authors: Group 25 - Muhammad Ali, Muhammad Tameem Mughal, Richard Milovanov
   Expected errors:
     1. undefined variable 'y' used in expression
     2. wrong argument count calling sum(1) instead of sum(1,2)
     3. variable 'a' declared as void
   Purpose: test semantic analysis for undefined variables, wrong arg count,
            and void variable declarations.
   date: mar 2026
*/

int sum(int a, int b)
{
  return a + b;
}

void main(void)
{
  int x;
  void bad;

  x = y + 1;
  x = sum(1);
  output(x);
}
